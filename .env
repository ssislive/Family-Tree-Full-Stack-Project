# ── Admin login password (change this!) ──
ADMIN_PASSWORD=changeme123

# ── Secret key used to sign JWT tokens (change this!) ──
JWT_SECRET=supersecretkey_change_this_too

# ── Server port ──
PORT=3000
